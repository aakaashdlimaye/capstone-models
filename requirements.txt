# Core numerics / data
numpy>=1.26
pandas>=2.2
pyarrow>=15
scipy>=1.11

# Classical ML baselines
scikit-learn>=1.4
xgboost>=2.0
imbalanced-learn>=0.12

# Deep learning.  Install from the CUDA index first if a GPU is present:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.4

# Interpretability
shap>=0.45

# Plotting / tests
matplotlib>=3.8
pytest>=8.0

# The exact versions the reported numbers were produced with are pinned in
# requirements.lock.txt.
