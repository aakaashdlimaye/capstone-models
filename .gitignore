.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Trained weights.  Regenerated by `python run_all.py`; Phase F is the only
# consumer and it rebuilds them from cache in seconds.
models/
logs/

# Pilot artefacts are a wiring check, never a reported number.
results_pilot/
reports_pilot/
scratch/

# Raw per-head, per-layer attention tensors: 1.3 GB, and an intermediate rather
# than a result.  Phase F reduces them to results/attention_by_quarter.csv and
# the figures, both of which are committed.  `python run_all.py --only c f`
# regenerates them.
results/attn/
